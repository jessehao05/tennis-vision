Workflow (no frontend):

- user adds video into "input_videos" folder
- user changes path names (input path, output path, shotmap output path) in main.py to match input video
- user runs "python main.py" in the terminal in the root directory of this project
- user finds their output video in "output_videos" and their output heatmap in "output_visuals"
